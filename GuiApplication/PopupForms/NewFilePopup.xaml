<Popup x:Class="GuiApplication.PopupForms.NewFilePopup"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GuiApplication.PopupForms"
             mc:Ignorable="d" 
             x:Name="PopupControl"
             d:DesignHeight="450" d:DesignWidth="800">

    <Border BorderBrush="Black" BorderThickness="1" Background="Beige">
    <StackPanel Orientation="Vertical" Margin="5">
            <WrapPanel Orientation="Horizontal">
                <Label Content="New File Name: " Margin="3,5,15,5"/>
                <Button Content="Create" Margin="5" Click="CreateButton_Clicked"/>
                <Button Content="Cancel" Margin="5,5,10,5" Click="CloseButton_Clicked"/>
            </WrapPanel>
            <WrapPanel Orientation="Horizontal">
                <TextBox Width="125" Margin="7,5,0,0" Text="{Binding ElementName=PopupControl, Path=NewFileName}"/>
                <TextBlock Text=".xml" Margin="3,5,0,0"/>
            </WrapPanel>
    </StackPanel>
    </Border>
</Popup>
